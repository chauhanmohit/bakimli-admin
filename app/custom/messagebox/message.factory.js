(function() {
    'use strict';

    angular.module('bakimliMessages').factory('Messages', [
        '$q', '$http', 'APIFormat', '$rootScope', messagesFactory
        ]);

    function messagesFactory($q, $http) { 
           
    }
})();
